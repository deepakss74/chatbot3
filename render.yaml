services:
  - type: web
    name: flask-ollama-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: SESSION_TYPE
        value: filesystem
